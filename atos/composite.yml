services:
  db:
    imgfeature: [postgres]
  web:
    imgfeature: [showcase]
constraints:
  - services['web'].alive()
  - varconst(services['web'], 'ThreadPerChild', iv1, varconst(services['web'], 'ThreadLimit', iv2, iv1.value < iv2.value))
outputs:
  big-thread-perchild:
    - varconst(services['web'], 'ThreadPerChild', iv1, iv1.value >= 100)
  small-thread-limit:
    - varconst(services['web'], 'ThreadLimit', iv1, iv1.value <= 64)

